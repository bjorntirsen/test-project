<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= channel.name %></title>
  <script defer type="text/javascript" src="../public/scripts/script.js"></script>
</head>
<body>
  <h1><%= channel.name %></h1>
  <h2><%= channel.description %></h2>
  <h3>Posts:</h3>
  <ul>
    <% channel.posts.forEach( post => { %>
    <li><%= post.by %>:<%= post.content %>:<%= post.date %></li>
    <% }) %>
  </ul>
  
  <form id="form" method="POST" action="/channel/<%= channel._id %>">
    <input type="text" name="by" placeholder="Username" id="by" autocomplete="off"><br>
    <textarea name="content" cols="30" rows="7" placeholder="Post" id="content" autocomplete="off"></textarea><br>
    <input type="submit" value="Post" id="submit" disabled>
  </form>
</body>
</html>